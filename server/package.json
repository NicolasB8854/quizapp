{
  "name": "quizapp-server",
  "version": "0.1.0",
  "private": true,
  "description": "AWS Lambda WebSocket handler for quizapp multiplayer",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "typescript": "~5.6.2"
  }
}
